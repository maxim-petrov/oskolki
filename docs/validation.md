# Проверка ветки с единственным оформлением

Ветка `feat/palace-cellar-only` выделена из `fix/palace-cellar-brightness` (`d7e2997`). В ней оставлено оформление «Дворец слов — подвал» с последней правкой яркости.

## Автоматическая проверка

- Проверка TypeScript, линтер и производственная сборка прошли.
- Все 29 существующих тестов движка и анимаций прошли: детерминированность, сдвиги, ограничения ресурсов, награды и магазин, сохранение, таймер, достижения, боевые события и полные маршруты.
- Сравнение с исходной веткой: игровые правила, анимация, атлас персонажей, его опорные точки и три PNG не изменены. Геометрия и палитра сохранились после удаления правил переключателя.
- В коде нет переключателя, чтения прежнего выбора оформления и инструмента WebMCP смены темы. Начальное оформление задано непосредственно в корневом документе. Ссылки на изображения ведут к существующим файлам.

## Локальная проверка

Локальная страница http://localhost:4531/ вернула HTTP 200. Браузерное тестирование и игровые действия при выделении ветки не выполнялись.
